<h1><%= params[:controller].humanize %> - <%= params[:action].humanize %> - <%= params[:method].humanize %></h1>

<%= form_tag(delete_test_relation_perform_path(encode_method(params[:method]), params[:option])) do |f| %>
  <%= render @partial if @partial.present? %>
  <%= render 'shared/query_methods_fields' %>
  <div class="actions">
    <%= submit_tag("#{params[:method].humanize} All types objects") %>
  </div>
<% end %>